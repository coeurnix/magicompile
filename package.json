{
  "name": "magicompile",
  "version": "0.0.0",
  "private": true,
  "license": "AC0",
  "scripts": {
    "build:tcc-wasm": "node ./scripts/build-tcc-wasm.mjs",
    "deploy": "npm run build:tcc-wasm && wrangler deploy",
    "dev": "npm run build:tcc-wasm && wrangler dev",
    "start": "npm run dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.83.0"
  }
}
